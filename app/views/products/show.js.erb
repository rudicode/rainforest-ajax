// TODO clear the review text box
// TODO animate the single new review.

$('#reviews').append("<%= j(render @product.reviews[-2]) %>")
$('#reviews p:last-child').hide().animate({
width: "toggle",
height: "toggle"
});
// $('#reviews p:last-child').animate({
// width: "toggle",
// height: "toggle"
// });
$('#review_comment').val('');
<%# binding.pry %>
//console.log('products#show.js.erb called.');