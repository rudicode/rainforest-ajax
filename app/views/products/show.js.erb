
// gets second last review ( last review will be nil b/c products#show needs
// to build a review to pass to the form_for.)
var newReview = $('<%= j(render @product.reviews[-2]) %>').hide();

$('#reviews').append(newReview)

$('#reviews p:last-child').animate({
height: "toggle"
});

$('#review_comment').val('');